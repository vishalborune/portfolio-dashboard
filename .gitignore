# Secrets — NEVER commit these (contain database keys, API keys)
.streamlit/secrets.toml
.env
*.key

# Cached thesis web-research notes (regenerable, and can be large)
.cache/

# Python
__pycache__/
*.pyc
.venv/
venv/
