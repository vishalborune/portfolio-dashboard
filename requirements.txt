# STEP 2 (fallback): eliminates curl_cffi completely by using the last
# requests-based yfinance (0.2.54). Use this ONLY if Step 1 still segfaults:
# rename this file's contents into requirements.txt (replace everything).
streamlit==1.59.1
pandas==3.0.3
numpy==2.5.1
pyarrow==25.0.0
yfinance==0.2.54
plotly==6.9.0
openpyxl==3.1.5
supabase==2.31.0
scipy==1.18.0
requests==2.34.2
websockets==15.0.1
tenacity==9.1.4
