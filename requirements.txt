# PINNED to the exact versions from the 11-Jul-2026 deploy that ran all day
# without issues (from the app's own install log). Streamlit Cloud runs
# Python 3.14, so these must be 3.14-compatible releases — do not downgrade
# below these, and do not use >= ranges (drift onto untested versions is what
# broke both apps on 12 Jul).
streamlit==1.59.1
pandas==3.0.3
numpy==2.5.1
pyarrow==25.0.0
yfinance==1.5.1
plotly==6.9.0
openpyxl==3.1.5
supabase==2.31.0
scipy==1.18.0
requests==2.34.2
websockets==15.0.1
tenacity==9.1.4
